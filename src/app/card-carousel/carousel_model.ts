export class carouselmodel  {
    upper: string;
    lower: string;
    descr: string;

    constructor(upper: string, lower: string, descr: string)   {
        this.upper = upper;
        this.lower = lower;
        this.descr = descr;
    }
}