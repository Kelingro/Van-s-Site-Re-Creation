export class pinkmodel  {
    gifs: string;
    title: string;
    subt: string;
    but: string;

    constructor(gifs: string, title: string, subt: string, but: string)   {
        this.gifs = gifs;
        this.title = title;
        this.subt = subt;
        this.but = but;
    }
}